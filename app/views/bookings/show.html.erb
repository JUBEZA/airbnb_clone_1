<h1>Booking Details</h1>

<p>
  <strong>Property:</strong> <%= link_to @booking.property.title, property_path(@booking.property) %>
</p>

<p>
  <strong>Start Date:</strong> <%= @booking.start_date %>
</p>

<p>
  <strong>End Date:</strong> <%= @booking.end_date %>
</p>

<p>
  <strong>Total Price:</strong> <%= number_to_currency(@booking.total_price) %>
</p>

<%= link_to 'Edit', edit_booking_path(@booking) %> |
<%= link_to 'Back', bookings_path %>
